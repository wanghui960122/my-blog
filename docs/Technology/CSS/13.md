---
title css display有哪些值？说明他们的作⽤。
---

# display有哪些值？说明他们的作⽤。

```css
block 块类型。默认宽度为⽗元素宽度，可设置宽⾼，换⾏显⽰。
none 缺省值。象⾏内元素类型⼀样显⽰。
inline ⾏内元素类型。默认宽度为内容宽度，不可设置宽⾼，同⾏显⽰。
inline-block 默认宽度为内容宽度，可以设置宽⾼，同⾏显⽰。
list-item 象块类型元素⼀样显⽰，并添加样式列表标记。
table 此元素会作为块级表格来显⽰。
inherit 规定应该从⽗元素继承 display 属性的值。
```