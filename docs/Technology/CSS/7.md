---
title css position的值
---

# position的值

```
1. absolute :⽣成绝对定位的元素， 相对于最近⼀级的 定位不是 static 的⽗元素来进⾏定位。
2. fixed （⽼IE不⽀持）⽣成绝对定位的元素，通常相对于浏览器窗⼝或 frame 进⾏定位。
3. relative ⽣成相对定位的元素，相对于其在普通流中的位置进⾏定位。
4. static 默认值。没有定位，元素出现在正常的流中
5. sticky ⽣成粘性定位的元素，容器的位置根据正常⽂档流计算得出
```