---
title css zoom:1的清除浮动原理?
---

# zoom:1的清除浮动原理?

```
清除浮动，触发hasLayout；
Zoom属性是IE浏览器的专有属性，它可以设置或检索对象的缩放⽐例。解决ie下⽐较奇葩的bug。
譬如外边距（margin）的重叠，浮动清除，触发ie的haslayout属性等。
来⻰去脉⼤概如下：
当设置了zoom的值之后，所设置的元素就会就会扩⼤或者缩⼩，⾼度宽度就会重新计算了，这⾥⼀
旦改变zoom值时其实也会发⽣重新渲染，运⽤这个原理，也就解决了ie下⼦元素浮动时候⽗元素不随
着⾃动扩⼤的问题。
Zoom属是IE浏览器的专有属性，⽕狐和⽼版本的webkit核⼼的浏览器都不⽀持这个属性。然⽽，
zoom现在已经被逐步标准化，出现在 CSS 3.0 规范草案中。
⽬前⾮ie由于不⽀持这个属性，它们⼜是通过什么属性来实现元素的缩放呢？
可以通过css3⾥⾯的动画属性scale进⾏缩放。
```