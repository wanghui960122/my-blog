---
title JS JavaScript是⼀⻔什么样的语⾔，它有哪些特点？
---

# JavaScript 是⼀⻔什么样的语⾔，它有哪些特点？

```js
javaScript⼀种直译式脚本语⾔，是⼀种动态类型、弱类型、基于原型的语⾔，内置⽀持类型。它的
解释器被称为JavaScript引擎，为浏览器的⼀部分，⼴泛⽤于客⼾端的脚本语⾔，最早是在HTML⽹⻚
上使⽤，⽤来给HTML⽹⻚增加动态功能。JavaScript兼容于ECMA标准，因此也称为ECMAScript。
基本特点
1. 是⼀种解释性脚本语⾔（代码不进⾏预编译）。
2. 主要⽤来向HTML（标准通⽤标记语⾔下的⼀个应⽤）⻚⾯添加交互⾏为。
3. 可以直接嵌⼊HTML⻚⾯，但写成单独的js⽂件有利于结构和⾏为的分离。
跨平台特性，在绝⼤多数浏览器的⽀持下，可以在多种平台下运⾏（如Windows、Linux、Mac、
Android、iOS等）。
```
